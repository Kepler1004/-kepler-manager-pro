// 프로젝트 루트 vercel.json 의 "crons" 배열에 아래 항목을 추가하세요.
// (파일이 없으면 새로 만들고, 이미 crons 가 있으면 배열에 이어 붙입니다.)
// 매일 00:10 UTC 실행 → 말레이시아(UTC+8) 기준 오전 8:10. 원하는 시각으로 조정하세요.

{
  "crons": [
    { "path": "/api/reports/rollover", "schedule": "10 0 * * *" }
  ]
}
