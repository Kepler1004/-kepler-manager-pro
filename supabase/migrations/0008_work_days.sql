-- ============================================================================
-- 0008_work_days.sql — 선생님/관리자 근무 요일 (휴가 근무일 계산용)
-- Supabase SQL Editor 에 붙여넣고 Run.
-- ============================================================================
-- 0=일 ... 6=토. 기본 월~금.
alter table profiles add column if not exists work_days smallint[] not null default '{1,2,3,4,5}';
